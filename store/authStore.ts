// Re-export from the canonical auth store.
// This file exists only as a compatibility shim — prefer importing from
// "@/store" or "@/store/auth.store" directly.
export { useAuthStore } from "./auth.store";
